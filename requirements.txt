# FotoQu — dependencies pipeline utama (classical CV enhancement)
# FQA & enhancement classical berjalan CPU-only (O(H*W)); GPU hanya untuk ArcFace.

# ── Core numerik ──────────────────────────────────────────────
numpy>=1.21.0
scipy>=1.7.0
pandas>=1.3.0
scikit-learn>=1.0.0

# ── Face detection + embedding (ArcFace 512-dim) ─────────────
insightface>=0.7.0
onnxruntime>=1.12.0          # ganti ke onnxruntime-gpu untuk GPU (ArcFace)

# ── Computer vision (FQA + enhancement classical) ─────────────
opencv-python>=4.5.0         # Laplacian, NLM, CLAHE, bicubic, Sobel/Tenengrad
Pillow>=9.0.0
pillow-heif>=0.6.0           # foto iPhone HEIC/HEIF

# ── Clustering ────────────────────────────────────────────────
hdbscan>=0.8.28              # CATATAN: UMAP TIDAK dipakai (arahan pembimbing)

# ── Retrieval (opsional, komparabilitas dgn baseline Rafiul) ──
faiss-cpu>=1.7.0

# ── Visualisasi & util ────────────────────────────────────────
matplotlib>=3.5.0
tqdm>=4.62.0

# ── Frontend ──────────────────────────────────────────────────
streamlit>=1.20.0

# ── Eksperimen pembanding DL (TERPISAH — hanya untuk notebooks/EXP_*) ──
# TIDAK diinstal untuk pipeline utama. Uncomment saat menjalankan notebook
# eksperimen restorasi DL di Colab (butuh GPU):
# torch>=2.0.0
# torchvision>=0.15.0
# basicsr>=1.4.2
# facexlib>=0.3.0
# codeformer            # atau: gfpgan>=1.3.8
